apiVersion: v1
kind: Service
metadata:
  name: add-task-v4-service
  namespace: argocd1
spec:
  type: LoadBalancer
  selector:
    app: add-task-v4
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: microtodo-add-v1-service
  namespace: argocd1
spec:
  type: LoadBalancer
  selector:
    app: microtodo-add-v1
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80   # ✅ FIXED (was 8080)